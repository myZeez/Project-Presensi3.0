window.PRESENSI_ADMIN_CONFIG = {
  // Untuk lokal, jalankan `npm start` dan pakai API di localhost:3000.
  // Untuk GitHub Pages, isi dengan URL API yang kamu host, contoh:
  // API_BASE_URL: 'https://api-presensi.example.com'
  API_BASE_URL: 'http://localhost:3000',
};
